// 正则表达式的扩展
{
	//es5
/*	let regex = new RegExp('xyz','i');
	let regex2 = new RegExp(/xyz/i);

		console.log(regex.test('xyz123'));  //true
	//es6
	let regex3 = new RegExp(/xyz/ig,'i');
		console.log(regex3.flags)  //flags是标识符，es06新增
*/}

{
/*	let s = 'bbb_bb_b';
	let a1 = /b+/g;
	let a2 = /b+/y;

		console.log(a1.exec(s),a2.exec(s))  //bbb，bbb
		console.log(a1.exec(s),a2.exec(s))  //bb，null
// g 是接上一次结果，忽略接下来匹配首字母_
// y 也是全局匹配，是严格下一个是匹配字母开始b
	// 检测开启y   	console.log(a1.sticky)
	*/
}

{
	// u  unicode字符编码
	// es5 中.只能匹配小于2个字节的字符
}

